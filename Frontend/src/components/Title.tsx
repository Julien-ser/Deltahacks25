import titleWithLogo from "../assets/titleWithLogo.png"; // Import the title logo

const Title = () => {
   return (
      <>
         <img src={titleWithLogo} alt="BinThere.AI Logo" id="title-image" />
      </>
   );
};

export default Title;
